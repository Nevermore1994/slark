plugins {
    id 'com.android.library'
}

android {
    namespace 'com.slark.sdk'
    compileSdk 33

    defaultConfig {
        minSdk 21
        targetSdk 33

        externalNativeBuild {
            cmake {
                path "../../../CMakeLists.txt"
                version "3.22.1"
            }
        }

        ndk {
            abiFilters 'arm64-v8a'
        }
    }

    externalNativeBuild {
        cmake {
            path "../../../CMakeLists.txt"
        }
    }
}